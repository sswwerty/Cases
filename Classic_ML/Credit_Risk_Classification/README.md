# Credit Scoring — проект по машинному обучению

## Описание проекта
В проекте решается задача бинарной классификации в области кредитного скоринга.

Цель — предсказать вероятность дефолта клиента по кредиту
на основе демографических, финансовых и поведенческих признаков.

Проект демонстрирует полный цикл работы с табличными данными:
от подготовки данных и обучения базовых моделей
до честного сравнения ансамблевых методов
и финальной оценки качества на отложенной тестовой выборке.

---

## Данные
Используется набор данных с информацией о клиентах, включающий:
- возраст, семейное положение;
- доходы, расходы и активы;
- кредитную историю;
- параметры займа и занятости.

Целевая переменная — статус кредита:
**default / ok**.

В проекте используется открытый датасет по кредитному скорингу из репозитория:
🔗 https://github.com/gastonstat/CreditScoring


---

## Подготовка данных
На этапе подготовки данных выполнено:
- обработка пропусков и аномальных значений;
- приведение категориальных признаков к строковому виду;
- one-hot кодирование категориальных признаков;
- разбиение данных на train / validation / test выборки;
- формирование матрицы признаков с единым пайплайном
  для обучения и оценки моделей.

Особое внимание уделено отсутствию утечек информации (data leakage):
все преобразования признаков корректно применяются
к обучающей, валидационной и тестовой выборкам.

---

## Модели
В проекте были обучены и проанализированы следующие модели:

- **Decision Tree**  
  Используется как базовая интерпретируемая модель.
  Анализ показал сильное переобучение,
  что делает её непригодной для финального использования.

- **Random Forest**  
  Ансамблевый метод, снижающий переобучение
  за счёт усреднения предсказаний множества деревьев.
  Показал устойчивое качество и хорошую обобщающую способность.

- **XGBoost**  
  Градиентный бустинг, широко применяемый в задачах кредитного скоринга.
  Для модели выполнен честный подбор гиперпараметров
  с использованием кросс-валидации на обучающей выборке.

Качество моделей оценивалось с помощью метрики **ROC-AUC**.

---

## Результаты

### Валидационная выборка
Валидационная выборка использовалась для анализа поведения моделей
и подбора гиперпараметров.

| Модель | ROC-AUC (validation) |
|------|----------------------|
| Decision Tree | ~0.78 |
| Random Forest | ~0.82 |
| XGBoost | ~0.83 |

---

### Тестовая выборка
Финальная оценка качества выполнена на отложенной тестовой выборке,
которая не использовалась на этапах обучения и настройки моделей.

| Модель | ROC-AUC (test) |
|------|----------------|
| Decision Tree | 0.783 |
| Random Forest | 0.831 |
| XGBoost | **0.836** |

На тестовой выборке модель **XGBoost** показала наилучшее качество
и была выбрана в качестве финального решения.

---

## Выводы
В рамках проекта:
- выполнена полная подготовка данных без утечек информации;
- проанализировано переобучение базовой модели;
- продемонстрировано преимущество ансамблевых методов;
- выполнен подбор гиперпараметров XGBoost
  с использованием cv;
- проведена финальная оценка качества моделей
  на отложенной тестовой выборке.

Итоговая модель **XGBoost** демонстрирует устойчивое качество
(ROC-AUC = 0.836 на тестовой выборке)
и может быть использована
как основа для дальнейшего развития скоринговой системы.

---

## Стек
- Python  
- Pandas, NumPy  
- Scikit-learn  
- XGBoost  
- Matplotlib